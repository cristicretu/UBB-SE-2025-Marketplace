<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MarketMinds.Views.HomePageView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MarketMinds.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="MarketMinds">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Padding="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Button Click="MarketMinds_Title_Click" 
                    Background="Transparent" 
                    BorderThickness="0">
            <TextBlock Text="MarketMinds" 
                          Style="{StaticResource TitleTextBlockStyle}" 
                          VerticalAlignment="Center"/>
            </Button>
            
            <CommandBar Grid.Column="1" 
                       DefaultLabelPosition="Right" 
                       Background="Transparent" 
                       IsOpen="False">
                <!-- Buyer-only buttons -->
                <AppBarButton x:Name="ChatBotButton" Label="ChatBot" Click="AppNavBarButton_Click">
                    <AppBarButton.Icon>
                        <FontIcon Glyph="&#xE8F2;" />
                    </AppBarButton.Icon>
                </AppBarButton>
                <AppBarButton x:Name="NotificationsButton" Label="Notifications" Click="AppNavBarButton_Click">
                    <AppBarButton.Icon>
                        <FontIcon Glyph="&#xEA8F;" />
                    </AppBarButton.Icon>
                </AppBarButton>
                
                <!-- Common button for all users -->
                <AppBarButton x:Name="ProfileButton">
                    <AppBarButton.Icon>
                        <FontIcon Glyph="&#xE77B;" />
                    </AppBarButton.Icon>
                    <AppBarButton.Flyout>
                        <MenuFlyout Placement="Bottom">
                            <MenuFlyoutItem Text="My Account" Tag="MyAccount" Click="ProfileMenuItem_Click">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE77B;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutItem x:Name="MyOrdersMenuItem" Text="My Orders" Tag="MyOrders" Click="ProfileMenuItem_Click">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE7BF;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutItem Text="My Reviews" Tag="MyReviews" Click="ProfileMenuItem_Click">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xF0E7;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutSeparator />
                            <MenuFlyoutItem Text="Logout" Tag="Logout" Click="ProfileMenuItem_Click">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xF3B1;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                        </MenuFlyout>
                    </AppBarButton.Flyout>
                </AppBarButton>

                <!-- Seller-only button -->
                <AppBarButton x:Name="CreateButton" Label="Create" Click="AppNavBarButton_Click">
                    <AppBarButton.Icon>
                        <FontIcon Glyph="&#xE710;" />
                    </AppBarButton.Icon>
                </AppBarButton>
                
                <!-- Buyer-only buttons -->
                <AppBarButton x:Name="WishlistButton" Label="Wishlist" Click="AppNavBarButton_Click">
                    <AppBarButton.Icon>
                        <FontIcon Glyph="&#xEB51;" />
                    </AppBarButton.Icon>
                </AppBarButton>
                <AppBarButton x:Name="CartButton" Label="Cart" Click="AppNavBarButton_Click">
                    <AppBarButton.Icon>
                        <FontIcon Glyph="&#xE7BF;" />
                    </AppBarButton.Icon>
                </AppBarButton>
            </CommandBar>
        </Grid>

        <!-- Content Frame -->
        <Frame x:Name="ContentFrame" Grid.Row="2"/>
    </Grid>
</Window>
